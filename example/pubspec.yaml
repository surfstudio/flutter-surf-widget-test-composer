name: surf_widget_test_composer_example
description: Example for the surf_widget_test_composer package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mocktail: ^1.0.1
  surf_widget_test_composer:
    path: ../

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
